enum Status {
  Uninitialized,
  Authenticated,
  Authenticating,
  Unauthenticated,
}

enum ViewState {
  idle,
  busy,
}
enum TaskType {
  task,
  calendarTask,
}

enum TypeButton { delete, edit, view }
