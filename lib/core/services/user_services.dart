class UserService {
  //Api _api = locator<Api>();

  // Future<ResponseModel> editUser(
  //   UserModel userModel,
  // ) async {
  //   // var response;
  //   // response = await _api.editUser(userModel);

  //  return ResponseModel.fromResponseEditUser();
  // }

  Future getUser({int id}) async {
    // var response;
    // response = await _api.getUser(id);

    // return response;
  }
}
